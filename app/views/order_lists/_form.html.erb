<%= form_with(model: order_list) do |form| %>
  <% if order_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_list.errors.count, "error") %> prohibited this order_list from being saved:</h2>

      <ul>
        <% order_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
# ToDo change forms with selectors and filters
  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :specialist_id %>
    <%= form.text_field :specialist_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
